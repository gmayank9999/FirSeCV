# Local planning/design notes — not part of the shipped product
docs/*
# ...except the product direction record, which explains why the code is shaped as it is
!docs/direction.md

# Dependencies / build output (future backend)
node_modules/
dist/
build/
*.log

# Bundled binaries (downloaded, not committed)
# backend/bin/

# Environment / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
